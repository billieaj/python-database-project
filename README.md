# python-database-project
2017 Python Database Project

Python 3 and SQLite 3.

Database contains Customer, Order, and Product tables in which data can be added, updated, searched for, or deleted.
User can recreate the tables if they wish, deleting all previously entered data.
